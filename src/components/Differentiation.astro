---
import { getLangFromUrl } from "@/i18n/utils";
import { translations } from "@/i18n";

const lang = getLangFromUrl(Astro.url);
const i18n = await translations[lang]();
---

<script src="../scripts/card3d.js"></script>

<div
  id="differentiation"
  class="max-w-6xl h-full text-white mx-auto flex flex-col gap-12 px-6 py-12"
>
  <!-- <h1
    data-aos="fade-up"
    data-aos-duration="1000"
    class="mt-4 font-normal text-[32px] text-white md:text-[45px] text-center lg:text-left mb-4"
  >
    <span class="text-[var(--color-title)]">+</span>
    {i18n.DIFFERENTIATION.TITLE}
  </h1>
  
  <div class="max-w-4xl mx-auto lg:mx-0 text-center lg:text-left">
    {i18n.DIFFERENTIATION.SUBTITLE ? (
      <p
        data-aos="fade-up"
        data-aos-duration="1000"
        class="text-lg lg:text-xl   leading-relaxed text-white/90"
        set:html={i18n.DIFFERENTIATION.SUBTITLE}
      />
    ) : null}
  </div>

  {i18n.DIFFERENTIATION.ITEMS && i18n.DIFFERENTIATION.ITEMS.length ? (
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {i18n.DIFFERENTIATION.ITEMS.map((item, index) => (
        <div
          data-aos="zoom-in"
          data-aos-duration="1000"
          data-aos-delay={index * 150}
          class="flex"
        >
          <div class="relative glass-effect bg-white/5 backdrop-blur-sm border border-white/25 p-6 rounded-lg hover:-translate-y-2 transition-all duration-300 shadow-lg hover:shadow-xl flex-1 group">
            <div class="text-center">
              <h3 class="text-white text-xl font-semibold mb-4 transition-colors duration-300">
                {item}
              </h3>
            </div>
            
            <div class="absolute -bottom-0.5 left-1/2 -translate-x-1/2 w-2/3 h-1 bg-[var(--color-title)] rounded-full opacity-60 group-hover:opacity-100 transition-opacity duration-300" />
            
            <div class="absolute inset-0 bg-gradient-to-r from-[var(--color-title)]/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 rounded-lg" />
          </div>
        </div>
      ))}
    </div>
  ) : null}
      {i18n.DIFFERENTIATION.TEXT ? (
      <p class="mt-3 text-white/80">{i18n.DIFFERENTIATION.TEXT}</p>
    ) : null} -->

  <div class="flex flex-col lg:flex-row-reverse gap-8 items-center">
    <div class="flex flex-col lg:w-1/2">
      <h1
        data-aos="fade-up"
        data-aos-duration="1000"
        class="mt-4 font-normal text-[32px] text-white md:text-[45px] text-center lg:text-right mb-4"
      >
        {i18n.DIFFERENTIATION.TITLE}
        <span class="text-[var(--color-title)]">+</span>
      </h1>
      <p
        data-aos="fade-left"
        data-aos-duration="1000"
        class="text-center lg:text-right text-lg lg:text-xl mb-6 pr-5"
      >
        {i18n.DIFFERENTIATION.SUBTITLE}
      </p>
      <ul class="space-y-2 text-lg pr-5">
        {
          i18n.DIFFERENTIATION.ITEMS.map((value) => (
            <li
              data-aos-delay="200"
              data-aos="fade-left"
              data-aos-duration="1000"
              class="flex items-center  gap-3 justify-end"
            >
              {value}
              <span
                class="rounded-full w-3 h-3"
                style="background-color: var(--color-title);"
              />
            </li>
          ))
        }
      </ul>
    </div>

    <div
      data-aos="zoom-in"
      data-aos-duration="1000"
      class="w-full lg:w-1/2 aspect-video relative scene px-4 lg:px-0"
    >
      <div
        class="card card-2 border-2 border-[var(--color-title)] rounded-br-4xl rounded-tl-4xl"
      >
        <div
          class="h-6 w-6 bg-[var(--color-title)] -bottom-3 -left-3 absolute decor decor-strong"
        >
        </div>
        <div
          class="h-6 w-6 bg-[var(--color-title)] -top-3 -right-3 absolute decor"
        >
        </div>

        <img
          src="/work/g.webp"
          alt="imagen"
          loading="lazy"
          class="rounded-br-4xl rounded-tl-4xl"
        />
      </div>
    </div>
  </div>
  <p data-aos="fade-in" data-aos-duration="1000" class="mt-3 text-lg text-center text-white">{i18n.DIFFERENTIATION.TEXT}</p>
</div>
<style>
  .scene {
    perspective: 1200px;
    -webkit-perspective: 1200px;
  }

  .card {
    transform-style: preserve-3d;
    -webkit-transform-style: preserve-3d;
    transition:
      transform 0.55s cubic-bezier(0.2, 0.9, 0.2, 1),
      box-shadow 0.55s;
    will-change: transform;
    transform-origin: center;
    position: relative;
    display: block;
    height: 100%;
    width: 100%;
    overflow: visible;
    backface-visibility: hidden;
  }

  .card-2 {
    transform: rotateX(5deg) rotateY(16deg);
    box-shadow: -30px 40px 60px rgba(179, 179, 179, 0.25);
  }
  .card-2:hover {
    transform: rotateX(0deg) rotateY(0deg);
    box-shadow: 0 20px 100px rgba(179, 179, 179, 0.25);
  }

  .decor {
    transform: translateZ(0); /* <- aquí la clave: no sobresalen en Z */
    -webkit-transform: translateZ(0);
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.18);
    will-change: transform;
    pointer-events: none;
  }

  .decor-strong {
    box-shadow: 0 10px 28px rgba(0, 0, 0, 0.22);
  }

  .card img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    -webkit-backface-visibility: hidden;
    backface-visibility: hidden;
  }
</style>