---
import { getLangFromUrl, useTranslation, slugify } from "@/i18n";
import Button_StrategicConversation from "@/components/Button_StrategicConversation.astro";
import Stars from "./Stars.astro";
const lang = getLangFromUrl(Astro.url);
const i18n = await useTranslation(lang);
---

<section
  id="team"
  class="mx-auto my-12 min-h-screen flex flex-col items-center justify-center h-full w-full pt-24 gap-8"
>
  <div class="-z-10 h-full w-full fixed top-0 left-0 pointer-events-none">
    <Stars />
  </div>
  <h2
    class="mb-8 font-bold text-4xl text-center text-white glass-effect w-full p-8"
  >
    {i18n.TEAM.TITLE}
  </h2>
  <div class="flex flex-col items-center justify-center text-white max-w-7xl gap-6 mb-12">
    <p class="text-center lg:text-left text-4xl font-semibold text-[var(--color-title)]">{i18n.TEAM.SUBTITLE}</p>
    <p class="text-center lg:text-left text-lg">{i18n.TEAM.TEXT}</p>
    <div class="grid grid-cols-2 gap-5 items-center justify-center max-w-3xl">
      {
        i18n.TEAM.CARDS.map((card) => (
          <div class="glass-effect border border-white/25 rounded-xl flex p-4 items-center justify-start">
            <div class="border border-white/15 bg-black/5 h-18 aspect-square rounded-lg p-3">
              <img
                src={card.ICON}
                alt={card.TEXT}
                loading="lazy"
                class="w-full h-full object-contain"
              />
            </div>
            <p class="px-4 text-xl font-semibold text-center lg:text-left">{card.TEXT}</p>
            <div class="absolute left-1/2 -translate-1/2 -bottom-1 h-1 w-2/3 bg-[var(--color-title)] rounded-full"></div>
          </div>
        ))
      }
    </div>
    <p class="text-center lg:text-left text-lg">{i18n.TEAM.TEXT_2}</p>
    <Button_StrategicConversation i18n={i18n} lang={lang} />
  </div>
  <div
    class="flex flex-wrap items-center justify-center gap-8 max-w-7xl mx-auto"
  >
    {
      i18n.TEAM.TEAM.map((director) => (
        <div data-aos="zoom-in">
          <a
            href={`/${lang}/our-team/${slugify(director.NAME)}`}
            class="group relative block h-full lg:w-70 aspect-square overflow-hidden rounded-sm bg-gradient-to-t from-gray-400 to-white shadow-lg transition-transform duration-300 hover:scale-105"
          >
            <img
              src={director.IMG}
              alt={director.NAME}
              class="w-full h-full object-contain grayscale-100 pointer-events-none"
            />
            <div class="right-3 bottom-3 left-3 absolute bg-[var(--color-bg-1)]/75 backdrop-blur-sm p-3 rounded-sm text-white text-center">
              <h3 class="mb-1 font-semibold text-base">{director.NAME}</h3>
              {/* <p class="text-xs uppercase tracking-wider">
                {director.POSITION}
              </p> */}
            </div>
          </a>
        </div>
      ))
    }
  </div>
</section>
